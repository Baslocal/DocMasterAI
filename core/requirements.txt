# DocMaster — Python Dependencies (all pinned with ==)
# Install path: /opt/docmaster/core/.venv/ (Python 3.12)
# STRICT RULE: All packages must be vendored — no PyPI at runtime.

# ── API Framework ──────────────────────────────────────────────────────────
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.6.0
pydantic-settings==2.2.0
python-multipart==0.0.9

# ── Database ───────────────────────────────────────────────────────────────
asyncpg==0.29.0
psycopg2-binary==2.9.9
SQLAlchemy==2.0.23
alembic==1.13.0

# ── Queue ──────────────────────────────────────────────────────────────────
redis==5.0.1

# ── Security / Auth ────────────────────────────────────────────────────────
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
cryptography==42.0.0

# ── Configuration ──────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Logging ───────────────────────────────────────────────────────────────
structlog==24.1.0

# ── HTTP Client ───────────────────────────────────────────────────────────
httpx==0.27.0
aiofiles==23.2.1

# ── OCR: Image preprocessing ──────────────────────────────────────────────
opencv-python-headless==4.9.0.80
Pillow==10.2.0
scikit-image==0.23.1
numpy==1.26.4
pyheif==0.8.0

# ── OCR: Engine 1 — Tesseract ─────────────────────────────────────────────
pytesseract==0.3.10

# ── OCR: Engine 2 — PaddleOCR ────────────────────────────────────────────
# NOTE: PaddleOCR MUST use local model paths — disable all auto-downloads
paddleocr==2.7.3
paddlepaddle==2.6.1

# ── OCR: Engine 3 — Kraken ───────────────────────────────────────────────
kraken==4.3.13

# ── Classifier ───────────────────────────────────────────────────────────
onnxruntime==1.17.0

# ── Vector / Embedding ───────────────────────────────────────────────────
langchain-text-splitters==0.0.1
tiktoken==0.6.0

# ── Development / Testing ─────────────────────────────────────────────────
pytest==8.0.0
pytest-asyncio==0.23.4
httpx==0.27.0
ruff==0.2.0
mypy==1.8.0
